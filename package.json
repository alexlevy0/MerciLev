{
  "name": "french-corrector-extension",
  "version": "1.0.0",
  "description": "Extension Chrome pour la correction orthographique et grammaticale en français",
  "scripts": {
    "build": "tsx build.ts",
    "test": "tsx run-tests.ts",
    "test:problematic": "tsx test-problematic-runner.ts",
    "dev": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.286",
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "tsx": "^4.20.4"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
